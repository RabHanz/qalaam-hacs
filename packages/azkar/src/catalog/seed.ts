/**
 * Backwards-compat shim: the original v0.1 seed lived here.
 * Real catalog now in `./index.ts` — split per-category with hadith grading.
 */
export { SEED_AZKAR, HISN_AL_MUSLIM } from './index.js';
